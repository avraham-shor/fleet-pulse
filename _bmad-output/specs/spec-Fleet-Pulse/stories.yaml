- id: "1"
  title: Repo scaffold and the shared constants module
  description: >-
    Stand up the Git repository, the Vite react-ts scaffold, Vitest, and the
    npm start / npm test scripts. Author shared/constants.js holding every
    tunable for both sides, per the SPEC constraint that no tunable literal
    lives anywhere else.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "2"
  title: server.js complete — contract and all eight failure modes
  description: >-
    Build CAP-1 in full: the brief's ten REST endpoints, the WS message set,
    the SSE stream, the 12-truck simulator, the quirk scheduler firing all
    eight failure modes, and the quarantined /api/dev/quirk/:id triggers.
    Includes the contract test asserting every emission parses against the
    wire-type declarations.
  spec_checkpoint: false
  done_checkpoint: true
  invoke_dev_with: ""

- id: "3"
  title: Wire contract types and the transport layer
  description: >-
    Declare src/contract/ verbatim from the brief, then build the two
    connection owners and the single mutation gate — sse-manager, ws-manager
    with register/keepalive/reconnect, and api-client injecting
    X-Dispatcher-Id and If-Match with one normalized error union.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "4"
  title: Store foundation and the telemetry integrity engine
  description: >-
    Build CAP-3 — the ordered ingest/classify pipeline, the per-signal trust
    envelopes, and the anomaly log — together with the store slices,
    boundedBuffer, the coalescing commit, and the one effective-trust
    selector. The mandated FR-6, FR-7, and FR-8 test cases land here.
  spec_checkpoint: true
  done_checkpoint: false
  invoke_dev_with: ""

- id: "5"
  title: Live fleet overview
  description: >-
    Build CAP-2 — the SVG coordinate grid, 12 markers with distinct status,
    newest-by-timestamp batch snapping with its sorted trail, and staleness
    badges. Establishes the widget registry, per-widget error boundaries, and
    the shared TrustBadge every later widget reuses.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "6"
  title: Dispatcher presence
  description: >-
    Build CAP-6 — registration over WS, the active-dispatcher list, the
    truck-viewing indicator including the explicit null clear, and the FR-19
    ghost rules with their three mandated tests. Precedes the route stories
    because mutations stay disabled while the dispatcher is unregistered.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "7"
  title: Route lifecycle management
  description: >-
    Build CAP-4 — create, transition, reassign, and cancel routes through the
    single mutation gate, with the WS echo as sole writer of the routes slice,
    confirmation on destructive actions, the FR-34 busy-truck guard, and the
    session-scoped audit trail. Conflict handling is story 8.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "8"
  title: Conflict avoidance and resolution
  description: >-
    Build CAP-5 — the side-by-side conflict chooser fed by the 409 body,
    reached identically by the stale-version and mid-processing race paths,
    plus the FR-31 inline notice that fires before a save is attempted. The
    brief's flagged senior-level design challenge; not cuttable.
  spec_checkpoint: true
  done_checkpoint: false
  invoke_dev_with: ""

- id: "9"
  title: Vehicle detail panel, resilience, and degraded mode
  description: >-
    Build CAP-7 and CAP-8 — the per-signal detail panel with sparklines over
    bounded history, route details and broadcast alerts, alongside
    Retry-After handling, the named-condition degraded banner with its clear
    hysteresis, socket reconnection, and the fleet_reset sequence.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

- id: "10"
  title: Observability and submission traceability
  description: >-
    Build CAP-9 — the dispatcher anomaly view and, once tier 1 is green, the
    tier-2 circuit breaker and developer metrics panel. Close CAP-10: README
    with the data flow and the tire-pressure extensibility example,
    DECISIONS.md, docs/, and the final PROMPTS.md entry.
  spec_checkpoint: false
  done_checkpoint: false
  invoke_dev_with: ""

---
topic: FleetPulse — real-time fleet dashboard whose contract is data trustworthiness: self-built mock server + React/TS client (telemetry integrity, routes & concurrency, presence, resilience, observability)
updated: 2026-08-18T16:54
---

- (note) input = structured, pre-sorted, final upstream set: PRD (final, FR-1..34 / NFR-1..9), PRD addendum, ARCHITECTURE-SPINE (final, AD-1..18); authored separation trusted, no elicitation needed
- (decision) slug inherited from source artifacts (prd-Fleet-Pulse-2026-08-18, architecture-Fleet-Pulse-2026-08-18) -> spec-Fleet-Pulse
- (decision) ARCHITECTURE-SPINE.md adopted as companion (owned by bmad-architecture, carries the 18 binding ADs + all diagrams) — referenced by relative path, never duplicated or edited here
- (decision) prd.md and addendum.md listed as sources: every load-bearing claim lifted into SPEC.md or a spec-authored companion; only narrative rationale (stack alternatives, incident backstory) stays behind
- (decision) kernel capabilities pitched at PRD group granularity (A-G + server + traceability), with per-FR line-item detail in requirements.md — 34 FRs would bloat the kernel past its shape
- (decision) four spec-authored companions: requirements.md (FR/NFR catalog + failure-mode map + scope tiers), trust-model.md (five trust states + classification policy), constants.md (tunables), test-matrix.md (mandated areas -> FR -> min cases)
- (capability) CAP-1 self-built mock server: brief contract + 8 failure modes; success = npm start runs it, contract test parses every emission, each mode fires deterministically
- (capability) CAP-2 live fleet overview (FR-1..4): 12 trucks, status, GPS-batch newest-by-timestamp + sorted trail, staleness badge
- (capability) CAP-3 telemetry integrity engine (FR-5..9): order-then-classify, per-signal trust envelope, bounded anomaly log
- (capability) CAP-4 route lifecycle (FR-10, 11, 14, 15, 34): create/transition/reassign/cancel, attributed, confirm on destructive and busy-truck
- (capability) CAP-5 concurrent-safe mutations + named conflict resolution (FR-12, 13, 31): one side-by-side chooser for both 409 paths, inline pre-save notice
- (capability) CAP-6 dispatcher presence (FR-16..19): id-keyed, ghost-disconnect safe, liveness timeout
- (capability) CAP-7 vehicle detail panel (FR-20..23, 32): per-signal trust carried through, bounded-history charts, broadcast alerts
- (capability) CAP-8 resilience + explicit degraded mode (FR-24..28, 33): Retry-After, breaker, named-condition banner with hysteresis, widget isolation, reset
- (capability) CAP-9 observability both audiences (FR-29, 30): dispatcher anomaly view + developer stream/socket metrics
- (capability) CAP-10 end-to-end traceability (G5, NFR-9): FR ids in commits, DECISIONS.md, test names; README data flow + tire-pressure example; PROMPTS.md kept live
- (constraint) 13 kernel constraints lifted: brief owns the wire contract; AD-1..18 binding; one constants module; trust assigned only in pipeline; CM1 fail-toward-alerting; CM2 inline not modal; no anonymous/unversioned writes; pessimistic UI; every collection bounded; untrusted server text; extensibility = registration; fixed stack no additions; CM3 depth over breadth + 2026-08-19 local-only deadline
- (decision) UJ-1 Dana/Yossi narrative kept as illustration only — its load-bearing claims (truck-level presence honest limit, avoidance-before-resolution, named chooser, audit trail) live in FR-18/FR-31/FR-13/FR-15
- (assumption) five kernel assumptions recorded: constants authored not observed; hand-rolled SVG suffices (OQ-1); clock skew negligible; reconnect issues fresh identity (audit may span two); panel-open history backfill is a build-time choice (OQ-2)
- (note) open_questions[] empty — all six PRD open questions closed: OQ-4/OQ-5 at the PRD gate, OQ-1/OQ-2/OQ-3/OQ-6 by the architecture spine (AD-14, AD-15, AD-2, AD-11)
- (event) self-validate pass 1 (coherence) PASS: 10 capabilities each carry intent+success; intents state WHAT (HOW sits in the adopted spine); all 13 constraints rule something out; 7 non-goals; success signal is a runnable two-tab demo script; CAP ids unique 1-10; prose lean
- (event) self-validate pass 2 (preservation) PASS: FR-1..34 and NFR-1..9 all present in requirements.md (verified by sweep); trust vocabulary + FR-7/FR-8 policies + two-clock rule in trust-model.md; full constants table both sides in constants.md; mandated test areas + structural assertions in test-matrix.md; goals, counter-metrics, failure-mode map, scope tiers, deliverables preserved; all 5 companion and 2 source paths resolve on disk
- (event) wrapper-only content dropped (on the record): PRD evaluation-parameter navigation table; PRD incident backstory prose beyond the Why; UJ-1 Dana/Yossi narration; addendum stack alternatives-considered rationale (Angular/RxJS, vanilla) — the decisions they justify are preserved, the justification prose is not
- (event) spec created: SPEC.md + 4 spec-authored companions + 1 adopted companion (ARCHITECTURE-SPINE.md); prd.md and addendum.md listed as sources
- (direction) user ran Story Breakdown; first approved a 14-story slicing, then revised toward consolidation mid-flow
- (note) correction on the record: the consolidation first offered (merge 2+3, 5+6, 13+14) yields 11 stories, not 9; a true 9 additionally merges routes+conflict and detail+resilience
- (decision) final slicing = 10 stories: all of the 9-shape mergers EXCEPT routes/conflict, which stay separate so the conflict-chooser review gate is not diluted by routine route CRUD
- (decision) story order: 1 scaffold+constants, 2 server.js complete, 3 contract+transport, 4 store+pipeline, 5 fleet overview, 6 presence, 7 route lifecycle, 8 conflict flow, 9 detail+resilience, 10 observability+docs
- (decision) presence (6) precedes route stories (7-8) because FR-16 disables mutations while the dispatcher is unregistered
- (decision) spec_checkpoint on story 4 (store+pipeline — the trust engine and the mandated tests) and story 8 (conflict flow — the brief-flagged senior challenge)
- (decision) done_checkpoint on story 2 only — dispatch pauses once server.js is complete and every failure mode can be fired by hand, before any client code lands
- (decision) invoke_dev_with left empty on every story: the binding detail already lives in SPEC.md and the four companions, so no dispatch note adds anything
- (event) stories.yaml schema check PASS: 10 entries, ids 1-10 as quoted strings, all required fields present, no status field, no prefix collision under the <id>- filename convention
- (event) story breakdown complete: 10 stories written to stories.yaml (not a companion, by convention)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/1-repo-scaffold-and-the-shared-constants-module.md`
  summary: Give `server.js` an env-var port override (fallback to `SERVER_PARAMS.SERVER_PORT`), and have `vite.config.ts` follow it.
  evidence: "**Closed by story 1.2.** `server.js`'s auto-start block reads `process.env.PORT` (validated, falling back to `SERVER_PARAMS.SERVER_PORT`); `vite.config.ts` computes the same fallback independently so both legs of `npm start` track one env var. Verified under `PORT=4123` against both the running server and the evaluated Vite config, and via a full `npm start` smoke test. See DECISIONS.md, Story 1.2."

## Deferred from: code review of story-1.1 (2026-08-18)

- No tsconfig will ever cover `server.js`, and nothing keeps `shared/` DOM-free. `tsconfig.app.json` owns `shared/` but is the DOM/browser project (`lib` includes `DOM`, `types: ["vite/client"]`); `tsconfig.node.json` includes only `vite.config.ts`. When `server.js` lands it will sit outside every TS project, and nothing checks that the module both sides import stays free of browser-only globals. Lands with story 1.2 — fix shape (extend the node project vs. a new `tsconfig.server.json`) depends on what 1.2 builds.
  **Closed by story 1.2.** `tsconfig.node.json`'s `include` now covers `server.js` and `server.contract.test.js` alongside `shared`; both typecheck clean under `strict`/`checkJs` (`@types/express`/`@types/ws` added dev-only to make that possible). `shared/` still sits in both `tsconfig.app.json` (DOM) and `tsconfig.node.json` (no DOM) — remains DOM-free by construction (it only exports plain object literals) and is now checked in both projects, which is strictly more coverage, not less.
- `npm start` does not run at HEAD while the story is closed. `package.json:11` runs `node --watch server.js` with no `server.js` in the tree; the story's frozen Approach claims a working `npm start`. Correctly sequenced against story 1.2 and honestly disclosed in `DECISIONS.md` — a scope/status mismatch, not a build defect.
  **Closed by story 1.2.** `server.js` exists and `npm start` boots both legs; verified end-to-end (server responds on `:3000`, Vite dev proxy on `:5173` forwards `/api` through to it).
- `create-vite` demo content ships as the application. `src/App.tsx` (counter, framework logos, Discord/Bluesky/X links), `src/App.css`, `src/index.css`, `public/icons.svg`, `src/assets/*` are untouched scaffold output behind a FleetPulse title and README. Explicitly scheduled for replacement in story 1.5 onward.
- No CI workflow runs `lint`/`build`/`test` on push. The scripts exist and `DECISIONS.md` records running all three by hand, but there is no `.github/workflows/`. For a repo graded as "the whole repository, not just the running app", an automated green/red signal is a gap — no story currently owns it.
- No coverage tooling. `@vitest/coverage-v8` is absent, there is no `test:coverage` script, and `vite.config.ts`'s `test` block sets only `environment`. First real coverage lands story 1.4 with the pipeline's mandated FR-cited cases.
- No `.env.example` despite gitignoring `.env` / `.env.local`. Nothing documents which variables exist or their defaults. Deferred because the env var name is story 1.2's to define — pair it with that story's port-fallback work.
  **Closed by story 1.2.** `.env.example` documents `PORT` (default 3000) and its rationale. No automatic `.env`-file loading is wired — a shell-exported `PORT` is the supported mechanism (see DECISIONS.md, Story 1.2, for why `--env-file` wasn't a clean fit across `concurrently`'s two child processes).
- No Prettier / `.editorconfig` / `.gitattributes`. On a Windows checkout with nine stories of code ahead, CRLF churn and style drift are unguarded, and the committed house style (no semicolons, single quotes) is enforced by nothing. Deferred because the spine's Consistency Conventions forbid added lint tooling — needs a spine amendment first if wanted.

## Deferred from: code review of story-1.2 (2026-08-18)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/2-server-js-complete-contract-and-all-eight-failure-modes.md`
  summary: `POST /api/dev/quirk/4` and `POST /api/dev/quirk/8` are the literal same function (`performSystemReassign`) — the two dev triggers are indistinguishable in effect, and the test titled for "quirk #4" (stale If-Match 409) never actually calls the quirk-4 trigger, only a hand-crafted stale header.
  evidence: The underlying behavior (stale-version 409 through the shared conflict shape, FR-12/FR-13) is genuinely covered by the hand-crafted-header test, so this isn't a functional gap — but the two dev-trigger endpoints being identical is undocumented and could mislead a future story or the defense interview into thinking quirks 4 and 8 fire independently. Worth clarifying (rename/document the shared mechanism) when routes/conflict stories (1.7/1.8) revisit this area.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/2-server-js-complete-contract-and-all-eight-failure-modes.md`
  summary: Quirk stacking (multiple independent `*_CHANCE` gates firing on the same truck in the same tick — e.g. a GPS batch that's also mid-fuel-glitch) is possible in the simulator but undocumented and untested.
  evidence: `GPS_BATCH_CHANCE`, `FUEL_GLITCH_CHANCE`, and `STUCK_SPEED_CHANCE` are rolled independently per truck per tick with no mutual-exclusion logic. Not a defect — the brief doesn't require quirks to be mutually exclusive, and testing every combination is out of proportion for this story — but worth a one-line note in `constants.md` confirming it's accepted behavior, for whichever later story first has to reason about combined-quirk readings.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/2-server-js-complete-contract-and-all-eight-failure-modes.md`
  summary: `PATCH /api/routes/:routeId` and `PUT /api/routes/:routeId/reassign` disagree on validation order for a terminal-status route with no `If-Match` header — PATCH returns `400 missing_if_match` (version check happens first), PUT-reassign returns `400 invalid_transition` (terminal check happens first, before any version check is required).
  evidence: Same conceptual violation (mutating a terminal route), different error codes depending on which endpoint is called. Neither path is currently tested, so it wasn't caught either way. Worth normalizing when route lifecycle (1.7) or conflict resolution (1.8) next touches these handlers.

## Deferred from: code review of story-1.3 (2026-08-18)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/3-wire-contract-types-and-the-transport-layer.md`
  summary: `api-client`'s `getFleet()` has no guard against overlapping concurrent calls — two in-flight calls share the same mutable `consecutiveFailures`/`breakerState`/`nextProbeAtMs` closure state and could interleave, jointly miscounting toward FR-25's 3-strike threshold or firing two simultaneous probes.
  evidence: Real given the shared mutable state, but unreachable today — no caller of `getFleet()` exists yet (`app/` and any polling/store logic land in stories 4+). The right fix (an in-flight promise cache, or leaving callers to serialize themselves) depends on the actual calling pattern the future consumer uses, so it's better decided by whichever story builds that consumer than guessed at here.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/3-wire-contract-types-and-the-transport-layer.md`
  summary: `api-client` has no injected validation seam — `ARCHITECTURE-SPINE.md`'s Consistency Conventions row (NFR-7) calls for "shared per-mutation validator functions in one `validators` module; forms call them pre-submit; `api-client` refuses payloads that fail them," but this story ships `createRoute`/`updateRouteStatus`/`reassignRoute`/`sendTruckAlert` with no such hook.
  evidence: Real gap against the architecture's stated intent, but `validators/` and the forms that would call it don't exist until the UI stories land (1.5+, concretely route creation in 1.7); server-side validation already guards correctness in the meantime (every mutating endpoint returns a typed 400 on bad input, confirmed in story 1.2). Client-side validation is a UX nicety best added alongside the forms that will actually invoke it, not guessed at from transport alone.

## Deferred from: code review of story-1.4 (2026-08-19)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/4-store-foundation-and-the-telemetry-integrity-engine.md`
  summary: `pipeline.ingestBackfill()` behaves inconsistently with `pipeline.ingest()` for fuel-signal historical replay — it over-logs a `fuel-suspect-resolved` anomaly for every historical 0% reading (no window carried across a stateless backfill call), where live mode would log at most one anomaly for the same underlying sequence; backfill also never seeds a signal's live classifier baseline (`SpeedState.lastPlausibleKmh` / `FuelState.lastTrustedPct`), so a truck whose first-ever *live* reading arrives right after its history has been backfilled can cold-start with "no trusted reading yet" (FR-5) despite the just-loaded history clearly containing a recent plausible value.
  evidence: Both behaviors are inherent, correct consequences of AD-4's "stateless plausibility only" rule for backfill (binding at the architecture level, not this story's to relax) — not a coding defect introduced by story 4. Neither is reachable today: nothing calls `pipeline.ingestBackfill()` in production yet (`app/` composition, which would wire it to the real history endpoint, doesn't exist until a later story). Worth resolving once that wiring lands — either by having `app/` seed classifier state from the last backfilled reading per signal before starting live ingest, or by explicitly documenting the anomaly-log volume difference as accepted behavior.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/4-store-foundation-and-the-telemetry-integrity-engine.md`
  summary: `computeEffectiveTrust` (the one effective-trust selector, AD-3) returns `null` ("no reading yet") for a truck/signal that has never reported telemetry, even when `health.isDegraded` is `true` — a truck that simply hasn't sent data yet during a known fleet-wide outage reads identically to ordinary cold-start, not as degraded.
  evidence: Real per the selector's own logic (`reading === null` short-circuits before the `isDegraded` check), but `health.isDegraded` is never populated from a real signal until story 1.9 (this story only ships the scaffold, `isDegraded: false` by default, per its own Boundaries) — so the combination can't occur in production yet. Revisit once story 1.9 wires real circuit-breaker/connection signals into the health slice; decide then whether "degraded" should outrank "no data yet" for display purposes.

## Deferred from: code review of story-1.5 (2026-08-19)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/5-live-fleet-overview.md`
  summary: `ErrorBoundary` has no reset/retry path — once a widget throws once, its fallback is permanent for the rest of the session; only a full page reload recovers it.
  evidence: Real, but FR-28's only requirement is containment ("the rest of the dashboard keeps working"), which is met and tested (siblings stay mounted) — self-healing after a transient throw is a legitimate future enhancement, not this story's stated scope. Revisit if a later widget (detail panel, 1.9) turns out to throw on recoverable/transient conditions rather than genuine bugs.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/5-live-fleet-overview.md`
  summary: `FleetOverview` has no "no trucks" empty-state message — if `GET /api/fleet` ever legitimately resolves to `[]`, the widget silently renders an empty grid and an empty roster list.
  evidence: Real gap in the render logic, but currently unreachable — the server's fleet size is the fixed constant `FLEET_SIZE = 12` (`shared/constants.js`), so an empty roster from a successful fetch can't happen today. Worth a one-line empty-state message if a future story ever makes fleet size configurable or filterable (the Tier-3 "filterable view" stretch goal would reopen this).
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/5-live-fleet-overview.md`
  summary: `FleetOverview`'s per-truck rows (`TruckMarkerGroup`/`TruckRosterRow`) aren't memoized, and `bounds`/`project` recompute from scratch on every render; since the widget subscribes to the whole `telemetry.trucks` record, a single truck's incoming reading re-renders and re-projects all 12 markers, visibly rescaling the map on every commit.
  evidence: Real UX/perf cost under live streaming (up to `RENDER_COALESCE_MAX_COMMITS_PER_SEC` = 10 commits/sec), and an inherent, correct consequence of this story's own approved auto-fit-from-live-bounds design (spec's Always: "The SVG grid auto-fits its viewBox from the live lat/lng range of the 12 trucks") — not a coding defect. Doesn't break any test or AC. Worth a follow-up perf pass (memoize `bounds`/`project` by value, `React.memo` the per-truck components) once more widgets exist and the real cost under the full coalescing ceiling is measurable, rather than guessing at a stabilization strategy now.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/5-live-fleet-overview.md`
  summary: No `aria-live` regions anywhere in the new UI — the fleet-unavailable message and per-truck stale/trust-badge transitions are silent to screen readers.
  evidence: Real accessibility gap, but no accessibility requirement (WCAG, screen-reader support, etc.) exists anywhere in this project's FRs/NFRs or the architecture spine — out of this story's scope to invent one unprompted. Revisit if a later requirements pass adds an accessibility bar.

## Deferred from: code review of story-1.6 (2026-08-19)

- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/6-dispatcher-presence.md`
  summary: There is a stale-`dispatcherId` race between calling `wsManager.register(name)` and the server's `registered` ack — `getDispatcherId()` keeps returning the pre-registration id until the ack lands, so any REST call made in that narrow window would carry a stale `X-Dispatcher-Id`.
  evidence: Real given `bootstrap.ts` now wires `wsManager.getDispatcherId` live into `apiClient` (previously the permanent `() => null` stub), but unreachable today — the only current caller, `startInitialFleetFetch`, runs once at boot before any `register()` call exists. Worth resolving once story 1.7's mutation UI adds callers that fire after a rename/re-register.
- source_spec: `_bmad-output/specs/spec-Fleet-Pulse/stories/6-dispatcher-presence.md`
  summary: No `aria-live` region on the new `PresencePanel` widget — dispatchers joining/leaving or changing their viewed truck update the DOM with no screen-reader announcement.
  evidence: Same accepted gap as story 1.5's identical finding — no accessibility requirement exists anywhere in this project's FRs/NFRs (NFR-8 covers only XSS-safe text-node rendering, not a11y), so inventing one here is out of scope. Revisit together if a later requirements pass adds an accessibility bar.

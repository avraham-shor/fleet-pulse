---
scope: FleetPulse full product: self-built server.js + React/TS client (telemetry integrity, routes+concurrency, presence, resilience, observability)
purpose: build-substrate
altitude: feature
updated: 2026-08-18T16:30
---

- (event) Headless create run. Driving input: PRD package prd-Fleet-Pulse-2026-08-18 (prd.md final, addendum.md, .memlog.md). Altitude: feature (spine keeps the epics coherent). Purpose: build-substrate. Repo is greenfield - no code, only BMad scaffolding. Submission due 2026-08-19.
- (constraint) Inherited from PRD/addendum, binding, not re-decided: React+TypeScript stack; server.js self-built with express+ws only, HTTP :3000, WS /ws, SSE GET /api/telemetry/stream, 12 trucks, all 8 failure modes, client-facing contract follows the assignment brief exactly and is never extended; If-Match optimistic locking + X-Dispatcher-Id on mutations; pessimistic UI (FR-12); five-state trust vocabulary; every collection bounded (NFR-3); tunable constants table PRD par.5; registration-not-modification extensibility (NFR-9); telemetry pipeline decoupled from UI (G2).
- (version) Verified npm latest 2026-08-18: react 19.2.8, vite 8.2.1, typescript 7.0.2, zustand 5.0.15, express 5.2.1, ws 8.21.3, vitest 4.1.10, @testing-library/react 16.3.2, concurrently 10.0.5. Node.js Active LTS: 24 (24.19.0). TS 7.0.2 is the native-port line; the Vite react-ts template scaffold governs the actual pin at npm install.
- (decision) Starter: Vite react-ts template (create-vite). Pre-decides build tooling, dev server, TS config; Vitest pairs natively. Next.js rejected: no SSR/routing need for a localhost dashboard; plain CRA-style webpack setups no longer the going choice.
- (decision) Paradigm: layered unidirectional dataflow with a pipes-and-filters telemetry core. Layers: transport -> pipeline -> store -> ui. Dependency rule: ui imports store (selectors) + api client only; pipeline is framework-free TS (no React, no DOM); transport knows sockets only and hands raw contract messages to pipeline/store; nothing imports upward. Prevents: UI reaching into sockets, pipeline coupled to rendering - the explicit G2 evaluation criterion.
- (decision) State: single Zustand store (v5) as the only client source of truth. All pipeline output lands through one coalesced batched commit path (render coalescing ceiling from shared constants, <=10 commits/sec global); components subscribe via selectors; no component-local mirrors of fleet state. Zustand over Redux Toolkit (lighter, store usable outside React for pipeline tests) and over hand-rolled (spends scarce hours, no evaluator payoff).
- (decision) OQ-1 resolved [ASSUMPTION - owner Avraham, decided headless]: fleet view renders on an SVG coordinate grid, no map library. Rationale: map quality explicitly ungraded; zero new dependencies; fully deterministic and unit-testable; no tile-network dependency so the defense demo cannot break offline; north star = trust over polish. Alternative weighed: Leaflet (real map for low effort, but adds external tiles + lib weight for no grading credit).
- (decision) OQ-2 resolved: detail-panel charts read the same bounded per-truck history the store already keeps for trails (~300 readings / 10 min). No history-backfill endpoint - adding one would extend the server contract, which is out of scope by PRD par.6. Panel shows whatever history has accumulated since app load; empty-history renders the FR-5 no-trusted-reading-yet state.
- (decision) OQ-3 resolved: shared/constants.js is the single source of truth for ALL tunables - server emission parameters (telemetry cadence 2s, batch size 10-30, fuel glitch 2-4s, stuck-sensor truck #7 999 km/h, ghost-disconnect delay <=10s) AND client thresholds (PRD par.5 table). Plain ESM object literals with JSDoc; package.json type:module; server.js imports it directly, client imports it through Vite, tests import the same file. No tunable literal may appear anywhere else.
- (decision) OQ-6 resolved: dev-only deterministic quirk triggers implemented as HTTP endpoints namespaced POST /api/dev/quirk/:id (force failure mode id now) plus POST /api/dev/reset (existing fleet_reset semantics). Contract-neutral by rule: client src/ must never reference /api/dev/* - only tests and defense-demo tooling call them. Enforceable by grep in review.
- (decision) Trust-state envelope: every value crossing the pipeline boundary is a Reading<T> {value, trust, readingTs, arrivalTs, raw?} where trust is one of the five PRD states. Only the pipeline assigns or changes trust; UI renders it verbatim and never re-derives plausibility. Raw stream values never reach the store or UI except inside the anomaly log entry.
- (decision) Pipeline stage order is fixed: ingest (parse contract msg) -> order/dedupe per truck by reading timestamp (FR-6, backfill-not-overwrite) -> classify (registered anomaly rules, FR-7/FR-8) -> single batched store commit. Clock rule: reading timestamps drive ordering and suspect windows; the arrival clock drives staleness (FR-4). No stage may run classification before ordering.
- (decision) Extensibility (NFR-9) is registry-based at three seams: signal registry (telemetry fields + their render/format meta), anomaly-rule registry (classifier functions the pipeline folds over), widget registry (dashboard panels the shell mounts inside error boundaries, FR-28). Adding a sensor/rule/widget = one new module + one register call; editing an existing module to accommodate it is a violation. README worked example (tire pressure) walks these three seams.
- (decision) Mutation protocol: all HTTP mutations flow through one api client module that injects X-Dispatcher-Id and If-Match, applies pessimistic UI (in-flight indicator, state changes only on 2xx), and routes every 409 into the single conflict-resolution flow (FR-13 side-by-side chooser fed by the 409 body: conflicting dispatcher identity + current server state). No component may call fetch directly.
- (decision) Connection lifecycle: exactly one SSE manager and one WS manager own connect/reconnect/backoff (1s doubling to 15s cap) and, for WS, re-register + presence rebuild on reconnect. Presence is keyed by server-issued dispatcherId, never name; liveness timeout 30s retires silent entries. The fleet HTTP endpoint is guarded by the circuit breaker inside the api client (3x503 opens, probe = max(10s, Retry-After)).
- (decision) Degraded mode: a health slice in the store holds named boolean conditions (telemetryStreamDown, fleetFetchFailing) each with its own set/clear rule + 5s clear hysteresis; only the transport/breaker layer sets them; the banner and age badges are pure renderings of the slice. No widget may set or infer degraded state on its own.
- (decision) Bounded collections: one boundedBuffer utility enforces caps at insertion for telemetry history, anomaly log, and audit trail; every cap value comes from shared/constants.js. A collection without a cap is a review defect (NFR-3).
- (decision) Server design: server.js is a single Node ESM file importing only express, ws, node builtins, and shared/constants.js. In-memory state only. Internal composition (fixed): truck simulator loop per 2s tick, quirk scheduler that injects the 8 failure modes per constants, route store with per-route version counters, presence registry, SSE broadcaster, WS hub. 409 bodies always carry conflictingDispatcher identity + current route state (FR-13); viewing_truck accepts null and the clear is broadcast (FR-18); dev triggers per OQ-6 force the quirk scheduler deterministically.
- (decision) Contract boundary: TS types mirroring the wire messages exactly (names per the assignment brief, no renaming/camelizing at the boundary) live in src/contract/ - the only place wire shapes are declared; server.js emits per the same shapes (authority: assignment PDF + shared constants). Client code beyond transport/pipeline consumes store types, not wire types.
- (decision) Operational envelope: local-only, no deploy/persistence. npm start = concurrently(node server.js, vite dev); Vite proxies /api and /ws to :3000 so client code uses same-origin paths; npm test = vitest run; npm run build stays functional but is not the graded path. Target browser: latest Chrome only. Node >= 24 LTS.
- (decision) Test conventions: Vitest; pipeline/store/breaker/presence tests are pure node-environment unit tests importing framework-free modules (the 16+ mandated cases live here); component tests (Testing Library + jsdom) only where a mandated behavior is UI-visible (conflict chooser, banner). Tests colocated *.test.ts next to the module. Emission params and thresholds imported from shared/constants.js - never re-hardcoded in tests.
- (assumption) Emission parameter values inside shared/constants.js (exact batch size distribution, quirk cadences, per-truck start positions) are chosen at build time within the brief's stated ranges; the spine fixes only that they live in shared constants and match PRD par.5 client thresholds.
- (question) TypeScript npm latest is 7.0.2 (native port). If the Vite react-ts scaffold still pins a 5.x, keep the scaffold's pin - do not hand-upgrade to 7 the day before submission. Resolve at npm install.
- (event) Reconcile pass ran: 2 parallel subagents (PRD+addendum; assignment PDF as contract authority). Lint clean. PDF contract extracted verbatim: 10 REST endpoints incl. GET /api/telemetry/history/:truckId and POST /api/reset; WS server->client registered/dispatcher_joined/dispatcher_left/dispatcher_viewing/route_assigned/route_updated/route_reassigned/truck_alert/fleet_reset/pong; client->server register_dispatcher/ping/viewing_truck. Findings 6 (PRD) + 10 (PDF), all applied below.
- (change) AD-15 corrected: the history endpoint IS in the brief - server.js must implement it; prior out-of-contract rationale was wrong. Invariant now: the store's bounded history is the only rendering source and any backfill flows through the pipeline ordering stage (FR-6 backfill) like live data; whether the client calls the endpoint on panel open is deferred to build. AD-11 corrected: the brief's POST /api/reset is the only reset; /api/dev/reset dropped; /api/dev/* restricted to quirk triggers.
- (change) AD-2 corrected to the brief's exact emission values: ghost disconnect = 20% chance with fixed 10s delay (not a 0-10s range); truck_7 stuck 999 km/h for 5-10s; fleet 503 15% chance with Retry-After. OQ-3 now genuinely closes at architecture: values are the brief's own, home is shared/constants.js; only free parameters (start positions, in-range distributions) remain build-time.
- (decision) If-Match scoped to version-bearing route mutations only (PATCH /api/routes/:routeId, PUT /api/routes/:routeId/reassign); POST /api/routes has no version - FR-34 warn-and-confirm covers that seam client-side. X-Dispatcher-Id stays on all mutations. api-client reads dispatcherId live from the session/presence slice and refuses mutations with a visible reason while unregistered (FR-16).
- (decision) Effective trust: pipeline assigns plausibility trust (trusted/suspect/sensor-fault); ONE store selector layers stale (per-truck arrival clock) then degraded (health slice) on top - the only trust source widgets read, preserving the PRD exactly-one-of-five rule. CM1 encoded as a rule: a classifier that cannot decide resolves toward alerting, never suppression; binds registered anomaly rules too.
- (decision) WS routing split: only SSE telemetry traverses the pipeline; WS coordination events (presence, routes, alerts) parse against contract/ in ws-manager and dispatch to slice actions. FR-19 ghost rules live in the presence slice; truck_alert entries land bounded in the fleet slice (FR-32). Unknown WS message types are dropped safely and counted in obs (FR-33). fleet_reset dispatches one store-wide reset action - wipes fleet/routes/presence/anomaly/history, refetches; breaker open triggers an immediate probe.
- (decision) WS keepalive: ws-manager sends ping at a constants-defined interval and consumes pong; ping/pong RTT is the FR-30 latency metric. Pre-breaker 503 policy: api-client auto-retries the fleet GET per Retry-After until the breaker opens (FR-24) - retrying is never a widget concern.
- (decision) Quirks self-fire per the brief's probabilities; POST /api/dev/quirk/:id is an additive deterministic override, never the only firing path. Wire truck ids are truck_1..truck_12, display Truck #N. The submission deliverable is a Git repository - git init is the first build step. Contract enforcement: one contract test asserts server emissions parse against contract/ declarations (contract/ is authoritative wherever the PDF is silent).
- (event) Reviewer gate ran: lint clean; rubric walker CONDITIONAL PASS (1 high, 3 medium, 5 low); tech-verification PASS WITH CORRECTIONS (all 10 npm pins re-verified live and current; TS scaffold pin is ~6.0.2 not 7.x; Vitest/RTL are manual adds; SSE client-abort through Vite proxy unreliable - server must reap on write error; Vitest 4 uses projects/per-file env not workspace); adversarial REVISE (C1 reset has no path into pipeline state, C2 route entity has two writers, H1-H7 incl. per-signal Reading granularity, backfill classification, session identity revocable by reset/liveness, no UI->WS send path, quirk-8 phantom attribution, anomaly log residence). Reviews in reviews/. All criticals/highs + mediums fixed as below; verdict after fixes: pass.
- (decision) C1 fix: reset is a three-step sequence owned by app/ - (1) pipeline.reset() drops the pending coalesce buffer, per-truck cursors, dedupe sets, open suspect windows; (2) store-wide reset action wipes fleet/routes/presence-cache/anomaly-log/history (obs counters and the session field survive); (3) refetch, immediate probe if breaker open. AD-1 gains the app/->pipeline lifecycle edge. Rubric H1 fix folded in: diagram arrows are the legal dependency directions; pipeline receives its commit sink, reset() and ingestBackfill() seams by injection from app/ so pipeline modules import only contract/ + constants.
- (decision) C2 fix - new AD-16: the server echoes every route event to ALL dispatchers, originator included; the WS route event is the SOLE writer of the routes slice; api-client 2xx only clears the in-flight indicator; routes slice writes are monotonic by version (<= current = counted no-op); the FR-15 audit trail is client-accumulated from WS route events only, session-scoped, no endpoint (late joiner sees a partial trail - accepted limit).
- (decision) H4/L5 fix - new AD-17: own dispatcherId lives in a dedicated session field written only by ws-manager (set on registered, cleared on socket loss only), exempt from the liveness sweep and the reset wipe. POST /api/reset resets fleet/route simulation only - the server presence registry survives, and after broadcasting fleet_reset the server re-announces current dispatchers via dispatcher_joined so wiped presence caches rebuild (FR-33 refetch satisfied with no new endpoint).
- (decision) H7 fix - new AD-18: the anomaly log is one obs-slice boundedBuffer with entry shape {ruleId, truckId, rawValue, readingTs, arrivalTs}; the pipeline emits entries only through the batched commit and retains no copy; FR-29 reads via selectors; reset wipes it.
- (decision) H2 fix: Reading envelopes are per-signal (speed, fuel, temperature, position each their own trust - FR-20); history is per-truck PER-SIGNAL boundedBuffers, caps per signal (~300 each) in shared constants - a GPS burst can no longer evict fuel/temp history. H3 fix: backfills bypass state update and window bookkeeping but still pass classification in stateless mode, so every history entry carries real plausibility trust and logged anomalies. H1 fix: api-client backfill enters via the injected pipeline.ingestBackfill() seam.
- (decision) H5 fix: ws-manager exposes a typed send-only facade (sendViewing) that ui/ may import; the FR-22 alert send stays REST (POST /api/fleet/:truckId/alert per the brief); transport subscribing to the store as a covert command channel is forbidden. H6 fix: failure mode 8 executes as a real reassignment by a permanently registered synthetic system dispatcher, present in presence and broadcast normally; 409 bodies carry the conflicting dispatcher id AND display name so the conflict view never depends on a presence lookup.
- (decision) Mediums: M1 - the coalescing ceiling governs telemetry commits plus dispatcher_viewing churn (rides the same scheduler); other WS events (route, join/leave, alert) commit directly, low-rate by contract; NFR-1 test counts coalesced commits. M2 - every parsed reading, accepted or rejected, refreshes the arrival clock: staleness means silence, not rejection. M3 - boundedBuffer takes an ordering key; history evicts from the timestamp-oldest end, sorted at commit never at render. Rubric M1 - shared per-mutation validators module, forms call pre-submit, api-client refuses failures. Rubric M2 - one constants-defined staleness tick started by app/; widgets never own timers. Rubric M3 - PDF fixes ghost at 20 percent chance / 10 s delay (PDF is authority; PRD up-to-10s is client tolerance wording, FR-19 unaffected). Lows: alert upserts truck stub (CM1); obs counters survive reset; capability map rows E/F/G gain their governing ADs; lint = scaffold oxlint as-is; TS row corrected to scaffold pin ~6.0.2; Vitest env split via per-file annotations; test stack noted as manual add; /ws proxied by exact prefix; express.json() mounted; server reaps SSE clients on write error.
- (event) Open-question triage: TS-pin question resolved by tech verification (live scaffold pins ~6.0.2; stack row corrected). Remaining [ASSUMPTION]: AD-14 SVG-grid/no-chart-lib (OQ-1, decided headless, owner Avraham - flip to Leaflet only if he overrides; every AD referencing it survives either way except AD-14 itself). No blockers. Renderings step: headless run, no additional human-facing artifact requested; skipped. External handoffs: none configured.
- (event) spine finalized - status final, 2026-08-18. 18 ADs, 12 convention rows, verified stack, 3 diagrams, capability map covering FR-1..34 + NFR-1..9 + server deliverable, 7 deferred items. All four PRD architecture-owned OQs closed (OQ-1 [ASSUMPTION], OQ-2, OQ-3, OQ-6). Full reviewer gate passed after fixes: lint clean, rubric conditional-pass conditions applied, tech corrections applied, all adversarial criticals/highs/mediums closed. Next: bmad-spec adoption or bmad-create-epics-and-stories; then build starts with git init + server.js story.
